## Attribute mapping for internalizing standoff-mode annotations into
## TEI

__standoff_special__splitId:
  prefix: xml
  name: xml:id
  valuePrefix: _

__standoff_special__prevId:
  name: prev
  valuePrefix: "#_"

type:
  name: ana

createdBy:
  name: resp

tagger:
  name: source
